services:
  rightsflow-metrics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rightsflow-metrics-app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
